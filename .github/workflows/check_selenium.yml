name: Check selenium server

on:
  workflow_dispatch:

permissions:
  contents: read
  
jobs:
  selenium:
    strategy:
      matrix:
        os: [macos-latest]
    runs-on: ${{ matrix.os }}
    name: check | os = ${{matrix.os}}
    steps:
      - name: Run checks
        run: |
          env
          brew list 
          brew list --cask